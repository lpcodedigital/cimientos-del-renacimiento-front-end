server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # servir archivos estáticos correctamente y fallback para rutas SPA
    location / {
        try_files $uri /index.html;
    }

    error_page 404 /index.html;
}
